# Copy this file to .env and fill values. Do NOT commit your .env.
# Mongo connection (use local or Atlas). Examples:
# MONGO_URI=mongodb://localhost:27017/
# MONGO_URI=mongodb+srv://<user>:<pass>@<cluster>/<db>?retryWrites=true&w=majority
MONGO_URI=mongodb://localhost:27017/
MONGO_DB=etl_db
COLLECTION_NAME=open_meteo_raw

# Optional: LOG_LEVEL can be DEBUG/INFO/WARNING/ERROR
LOG_LEVEL=INFO

# If you switch to an API that needs a key, add it here and read via os.getenv in your code.
# API_KEY=your_api_key_here
